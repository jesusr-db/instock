bundle:
  name: instockcv

variables:
  catalog:
    default: main
  schema:
    default: instockcv
  schema_dev:
    default: instockcv_dev

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: ${workspace.host}

  prod:
    mode: production
    workspace:
      host: ${workspace.host}
